\section{Utlity\-Func.\-h}
\label{_utlity_func_8h}\index{D\-:/\-Out\-\_\-\-Of\-\_\-\-Core\-\_\-\-Git/src/\-Utlity\-Func.\-h@{D\-:/\-Out\-\_\-\-Of\-\_\-\-Core\-\_\-\-Git/src/\-Utlity\-Func.\-h}}

\begin{DoxyCode}
00001 \textcolor{preprocessor}{#ifndef \_UTILITY\_FUNC\_H}
00002 \textcolor{preprocessor}{}\textcolor{preprocessor}{#define \_UTILITY\_FUNC\_H}
00003 \textcolor{preprocessor}{}
00004 \textcolor{preprocessor}{#include <string>}
00005 
00006 \textcolor{keyword}{inline} \textcolor{keywordtype}{size\_t} make\_upper\_four\_multiply(\textcolor{keywordtype}{size\_t} number)
00007 \{
00008         \textcolor{comment}{//number % 4 != 0}
00009         \textcolor{keywordflow}{if}(number & 0x00000003) \textcolor{keywordflow}{return} ((number >> 2) + 1) << 2;
00010 
00011         \textcolor{comment}{//number is the multiply of 4, then just get the number itself}
00012         \textcolor{keywordflow}{return} number;
00013 \}
00014 
00015 \textcolor{keyword}{inline} \textcolor{keywordtype}{size\_t} make\_less\_four\_multiply(\textcolor{keywordtype}{size\_t} number) 
00016 \{
00017         \textcolor{comment}{//number % 4 != 0}
00018         \textcolor{keywordflow}{if}(number & 0x00000003) \textcolor{keywordflow}{return} ((number >> 2) << 2);
00019 
00020         \textcolor{comment}{//number is the multiply of 4, then just get the number itself}
00021         \textcolor{keywordflow}{return} number;
00022 \}
00023 
00024 \textcolor{keyword}{inline} \textcolor{keywordtype}{size\_t} get\_least\_order\_number(\textcolor{keywordtype}{size\_t} number)
00025 \{
00026         \textcolor{keywordflow}{while}(number & (number - 1)) \{
00027                 number &= (number - 1);
00028         \}
00029         \textcolor{keywordtype}{int} count = 0;
00030         \textcolor{keywordflow}{while}(number) \{
00031                 ++count;
00032                 number >>= 1;
00033         \}
00034 
00035         \textcolor{keywordflow}{return} (count < 1) ? 0 : (count - 1);
00036 \}
00037 
00038 \textcolor{preprocessor}{#endif}
\end{DoxyCode}
